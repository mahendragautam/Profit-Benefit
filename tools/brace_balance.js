const fs=require('fs');const s=fs.readFileSync('temp_dyn.js','utf8');const lines=s.split('\n');let count=0;for(let i=0;i<lines.length;i++){const ln=lines[i];for(let j=0;j<ln.length;j++){const ch=ln[j];if(ch==='/'){const next=ln[j+1];if(next==='/' ) {break;} if(next==='*'){ /* ignore */ }} if(ch==="'"||ch==='"'||ch==='`'){ /* skip string naive: skip to next same char */ let k=j+1; while(k<ln.length && ln[k]!==ch){ if(ln[k]==='\\') k+=2; else k++; } j=k; continue;} if(ch==='{') count++; else if(ch==='}') count--; } if(i<260 && (i<20 || (count<0) || (i>40 && i<260 && (i%10===0)))) console.log((i+1).toString().padStart(4)+': count='+count+' | '+lines[i]); }console.log('final count',count);
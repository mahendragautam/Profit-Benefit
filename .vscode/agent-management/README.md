Agent-management helper folder (tracked placeholder)

This folder contains configuration and helper files for the local VS Code agent-management extension.

- The runtime report files (`.vscode/agent-reports/*.json`) are generated and should not be tracked.
- Use this folder to add tracked configuration or extension helpers that should be stored in the repo.
